# 方法一：使用绝对路径（推荐，最不容易出错）
# 注意这里必须是 G:/ 开头，且全用正斜杠 /K
path: /data01/FL_Grp/WangHao/wh/datasets/COD10K

# train 和 val 写相对于上面 path 的相对目录
train: images/train
val: images/val # ⚠️请打开你的文件夹确认一下，这里到底叫 val 还是 test？

# 类别
nc: 1
names:
  0: camouflaged
